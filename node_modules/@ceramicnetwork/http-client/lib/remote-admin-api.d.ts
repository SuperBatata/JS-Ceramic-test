import { AdminApi, PinApi, NodeStatusResponse } from '@ceramicnetwork/common';
import { StreamID } from '@ceramicnetwork/streamid';
import { DID } from 'dids';
export declare class RemoteAdminApi implements AdminApi {
    private readonly _apiUrl;
    private readonly _getDidFn;
    private readonly _fetchJson;
    private readonly _pinApi;
    readonly modelsPath = "./admin/models";
    readonly getCodePath = "./admin/getCode";
    readonly nodeStatusPath = "./admin/status";
    constructor(_apiUrl: URL, _getDidFn: () => DID);
    private getCodeUrl;
    private getModelsUrl;
    private getStatusUrl;
    private buildJWS;
    private generateCode;
    nodeStatus(): Promise<NodeStatusResponse>;
    startIndexingModels(modelsIDs: Array<StreamID>): Promise<void>;
    getIndexedModels(): Promise<Array<StreamID>>;
    stopIndexingModels(modelsIDs: Array<StreamID>): Promise<void>;
    get pin(): PinApi;
}
//# sourceMappingURL=remote-admin-api.d.ts.map